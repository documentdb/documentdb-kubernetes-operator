name: govulncheck

on:
  push:
    branches: [main]
    paths:
      - 'operator/src/**/*.go'
      - 'operator/src/go.mod'
      - 'operator/src/go.sum'
      - 'documentdb-kubectl-plugin/**/*.go'
      - 'documentdb-kubectl-plugin/go.mod'
      - 'documentdb-kubectl-plugin/go.sum'
      - 'operator/cnpg-plugins/sidecar-injector/**/*.go'
      - 'operator/cnpg-plugins/sidecar-injector/go.mod'
      - 'operator/cnpg-plugins/sidecar-injector/go.sum'
      - 'test/e2e/**/*.go'
      - 'test/e2e/go.mod'
      - 'test/e2e/go.sum'
      - 'test/longhaul/**/*.go'
      - 'test/longhaul/go.mod'
      - 'test/longhaul/go.sum'
      - 'test/shared/**/*.go'
      - 'test/shared/go.mod'
      - 'test/shared/go.sum'
  pull_request:
    paths:
      - 'operator/src/**/*.go'
      - 'operator/src/go.mod'
      - 'operator/src/go.sum'
      - 'documentdb-kubectl-plugin/**/*.go'
      - 'documentdb-kubectl-plugin/go.mod'
      - 'documentdb-kubectl-plugin/go.sum'
      - 'operator/cnpg-plugins/sidecar-injector/**/*.go'
      - 'operator/cnpg-plugins/sidecar-injector/go.mod'
      - 'operator/cnpg-plugins/sidecar-injector/go.sum'
      - 'test/e2e/**/*.go'
      - 'test/e2e/go.mod'
      - 'test/e2e/go.sum'
      - 'test/longhaul/**/*.go'
      - 'test/longhaul/go.mod'
      - 'test/longhaul/go.sum'
      - 'test/shared/**/*.go'
      - 'test/shared/go.mod'
      - 'test/shared/go.sum'
  schedule:
    - cron: '22 10 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  govulncheck:
    name: govulncheck
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        module:
          - operator/src
          - documentdb-kubectl-plugin
          - operator/cnpg-plugins/sidecar-injector
          - test/e2e
          - test/longhaul
          - test/shared
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-go@v5
        with:
          go-version-file: ${{ matrix.module }}/go.mod

      - name: Run govulncheck
        working-directory: ${{ matrix.module }}
        run: go run golang.org/x/vuln/cmd/govulncheck@latest ./...
